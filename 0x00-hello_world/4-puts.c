#include <stdio.h>
/**
 * main- serves as the main entry point for your program
 * Return: zero if no errors otherwise returns non-zero value.
 */
int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}

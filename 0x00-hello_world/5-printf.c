#include <stdio.h>
/**
 * main- is the entry point for the program.
 * Return: zero if no error, and non zero if there is error
 */
int main(void)
{
	printf("with proper grammer, but the outcome is a piece of art,\n");
	return (0);
}

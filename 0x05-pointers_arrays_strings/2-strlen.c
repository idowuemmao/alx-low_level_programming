#include "main.h"

/**
 * _strlen -a function that returns the lenght of a string
 * Done by EMmygreat
 * @s: char to check
 *
 * Return: 0
 */

int _strlen(char *s)
{
	int a = 0;

	while (s[i] != '\0')
	{
		i++;
	}

	return (i);
}

#include "main.h"

/**
 * _abs - used to calculate the
 * absolute value of an integer
 * @n: The integer to be computed
 * Done by emmygreat
 * Return: The absolute value of the integer
 */

int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
